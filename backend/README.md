# Backend

Minimal FastAPI service scaffold for the Kodnexa platform. Future API, graph, RAG, and service modules belong under `app/`.

## Run

```bash
pip install -r requirements.txt
uvicorn main:app --reload
```
