import { Paperclip, Mic, Send } from 'lucide-react'
import { useEffect, useRef, useState } from 'react'
export default function ChatInput({ onSend }) { const [value,setValue]=useState(''); const input=useRef(null); useEffect(()=>input.current?.focus(),[]); const submit=()=>{if(value.trim()){onSend(value.trim());setValue('')}}; return <div className="border-t border-white/10 p-3"><div className="flex items-end gap-2 rounded-2xl border border-white/10 bg-ink/70 px-2 py-2 focus-within:border-indigo-400/50"><button aria-label="Attach file" className="grid h-8 w-8 place-items-center text-slate-500"><Paperclip className="h-4 w-4" /></button><textarea ref={input} value={value} onChange={e=>setValue(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();submit()}}} rows="1" placeholder="Ask anything..." className="max-h-24 flex-1 resize-none bg-transparent py-1.5 text-sm text-white outline-none placeholder:text-slate-500" /><button aria-label="Voice input" className="grid h-8 w-8 place-items-center text-slate-500"><Mic className="h-4 w-4" /></button><button aria-label="Send message" onClick={submit} className="grid h-8 w-8 place-items-center rounded-full bg-gradient-to-br from-indigo to-violet text-white shadow-glow"><Send className="h-4 w-4" /></button></div></div> }
