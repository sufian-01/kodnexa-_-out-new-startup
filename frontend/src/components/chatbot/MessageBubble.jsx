import { motion } from 'framer-motion'
import robot from '../../assets/chatbot/robot.png'
export default function MessageBubble({ message }) { const assistant = message.role === 'assistant'; return <motion.div initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} className={`flex items-end gap-2 ${assistant ? 'justify-start' : 'justify-end'}`}>{assistant && <img src={robot} alt="Kodnexa AI" className="h-7 w-7 rounded-full border border-indigo-300/30 object-cover" />}<div className={`max-w-[85%] rounded-2xl px-4 py-3 text-sm leading-6 ${assistant ? 'rounded-tl-sm border border-white/10 bg-white/[.055] text-slate-200' : 'rounded-tr-sm bg-gradient-to-r from-indigo to-violet text-white shadow-glow'}`}><p className="whitespace-pre-line">{message.text}</p><span className={`mt-1 block text-[10px] ${assistant ? 'text-slate-500' : 'text-white/60'}`}>{message.time}</span></div></motion.div> }
