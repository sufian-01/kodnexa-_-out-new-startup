const targets = { 'Our Services': '/#services', Pricing: '/#services', 'Upcoming Hackathons': '/hackathon', 'Contact Us': '/contact', 'Book Meeting': '/contact', 'Placement Support': '/services', 'AI Solutions': '/services', Automation: '/services', 'Website Development': '/services' }
const highlight = () => { const id = location.hash.slice(1); const target = id && document.getElementById(id); if (target) { target.scrollIntoView({ behavior: 'smooth', block: 'start' }); target.classList.add('ring-2','ring-cyan/60','ring-offset-4','ring-offset-ink'); setTimeout(() => target.classList.remove('ring-2','ring-cyan/60','ring-offset-4','ring-offset-ink'), 1400) } }
export function handleQuickAction(action) { const destination = targets[action] || '/services'; if (destination.startsWith('/#') && location.pathname === '/') { history.replaceState(null, '', destination); highlight(); return } location.href = destination; if (action === 'Book Meeting') setTimeout(() => document.querySelector('form input, form textarea')?.focus(), 450) }
