import { useEffect, useState } from 'react'

const getTime = (target) => { const remaining = Math.max(0, new Date(target).getTime() - Date.now()); return { Days: Math.floor(remaining / 86400000), Hours: Math.floor(remaining / 3600000) % 24, Minutes: Math.floor(remaining / 60000) % 60, Seconds: Math.floor(remaining / 1000) % 60 } }
export default function Countdown({ target, className = '' }) { const [time, setTime] = useState(() => getTime(target)); useEffect(() => { const id = setInterval(() => setTime(getTime(target)), 1000); return () => clearInterval(id) }, [target]); return <div className={`grid grid-cols-4 gap-2 ${className}`}>{Object.entries(time).map(([label, value]) => <div key={label} className="rounded-xl border border-white/10 bg-ink/60 py-3 text-center"><b className="block font-display text-xl text-white sm:text-2xl">{String(value).padStart(2, '0')}</b><span className="mt-1 block text-[9px] uppercase tracking-widest text-slate-500">{label}</span></div>)}</div> }
