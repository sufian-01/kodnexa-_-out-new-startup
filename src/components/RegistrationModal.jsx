import { AnimatePresence, motion } from 'framer-motion'
import { CheckCircle2, X } from 'lucide-react'
import { useState } from 'react'
import Button from './Button'

const fields = [['fullName','Full Name','text'],['email','Email','email'],['phone','Phone Number','tel'],['college','College','text'],['university','University','text'],['year','Year','text'],['branch','Branch','text'],['city','City','text'],['github','GitHub','url'],['linkedin','LinkedIn','url'],['team','Team Name','text'],['members','Number of Members','number']]
export default function RegistrationModal({ open, onClose }) { const [submitted, setSubmitted] = useState(false); const [error, setError] = useState(''); const submit = (e) => { e.preventDefault(); if (!e.currentTarget.reportValidity()) return; setSubmitted(true) }; return <AnimatePresence>{open && <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-[100] flex items-end bg-ink/80 p-3 backdrop-blur-sm sm:items-center sm:justify-center" role="dialog" aria-modal="true" aria-labelledby="registration-title"><motion.div initial={{ y: 24, scale: .98 }} animate={{ y: 0, scale: 1 }} exit={{ y: 24, scale: .98 }} className="max-h-[94vh] w-full max-w-3xl overflow-y-auto rounded-3xl border border-white/10 bg-panel p-6 shadow-2xl sm:p-8"><button onClick={onClose} className="float-right grid h-9 w-9 place-items-center rounded-lg text-slate-400 hover:bg-white/5 hover:text-white" aria-label="Close registration form"><X className="h-5 w-5" /></button>{submitted ? <div className="grid min-h-[380px] place-items-center text-center"><div><CheckCircle2 className="mx-auto h-14 w-14 text-cyan" /><h2 className="mt-5 font-display text-3xl text-white">You’re registered!</h2><p className="mt-3 text-slate-400">We’ll send event details to your inbox shortly.</p><Button onClick={onClose} className="mt-7">Close</Button></div></div> : <><p className="eyebrow">Hackathon 2026</p><h2 id="registration-title" className="font-display text-2xl text-white sm:text-3xl">Reserve your build spot.</h2><p className="mt-2 text-sm text-slate-400">Complete your details to register for Kodnexus AI Hackathon 2026.</p><form onSubmit={submit} className="mt-6 grid gap-4 sm:grid-cols-2">{fields.map(([name,label,type]) => <label key={name} className="text-xs font-semibold uppercase tracking-wider text-slate-400">{label}<input required type={type} name={name} min={name === 'members' ? 1 : undefined} className="mt-2 w-full rounded-xl border border-white/10 bg-ink px-3 py-3 text-sm text-white outline-none focus:border-indigo" /></label>)}<label className="sm:col-span-2 text-xs font-semibold uppercase tracking-wider text-slate-400">Project Idea<textarea required name="idea" rows="3" className="mt-2 w-full rounded-xl border border-white/10 bg-ink px-3 py-3 text-sm text-white outline-none focus:border-indigo" /></label><label className="text-xs font-semibold uppercase tracking-wider text-slate-400">Experience Level<select required name="experience" className="mt-2 w-full rounded-xl border border-white/10 bg-ink px-3 py-3 text-sm text-white outline-none focus:border-indigo"><option value="">Select level</option><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></label><label className="flex items-center gap-3 self-end pb-3 text-sm text-slate-300"><input required type="checkbox" className="h-4 w-4 accent-indigo" /> I accept the Terms & Conditions</label>{error && <p className="text-sm text-rose-400 sm:col-span-2">{error}</p>}<Button type="submit" className="sm:col-span-2">Submit registration</Button></form></>}</motion.div></motion.div>}</AnimatePresence> }
